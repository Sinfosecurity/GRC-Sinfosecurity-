# Release certification

Gates are evaluated with evidence in the transformation report. A gate is PASS only with objective proof (tests, build output, or explicit NOT_CONFIGURED behavior).

This branch is **not** declared PRODUCTION CANDIDATE until every P0 gate has that evidence.

See the final transformation report for the current table.
