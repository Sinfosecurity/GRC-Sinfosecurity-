# Docker Compose Environment Variables
# Copy this file to .env in the root directory and update with secure values

# PostgreSQL Database Credentials
POSTGRES_USER=grc_user
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE
POSTGRES_DB=grc_platform

# MongoDB Database Credentials
MONGO_USER=grc_user
MONGO_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE
MONGO_DB=grc_documents

# Redis Cache
REDIS_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE

# IMPORTANT: Generate secure random passwords for production:
# openssl rand -base64 32
